environment: development

logging:
  level:
    root: INFO
    org.hibernate.SQL: DEBUG

spring:
  datasource:
    url: jdbc:h2:mem:se452dev2
  jpa:
    generate-ddl: true
    show-sql: true
    defer-datasource-initialization: true