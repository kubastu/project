package com.group2.project.jsonmanipulator;
/**
 * <b>This package defines Java classes that are responsible for reading and writing information from the calendar to a persistent json file.
 * <ul>
 * <li> {RawJSONParser} defines a static Java class that reads information from a json file with a specified directory. Parses the json file, and sends result back to CalendarController.
 * <li> {RawJSONWriter} defines a static Java class that writes information from the CalendarController to the specified directory for a json file.
 * </ul>
 * @author Jakub Zywicki
 */