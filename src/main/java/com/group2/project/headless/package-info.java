package com.group2.project.headless;
/**
 * <b> This package defines Java Headless code designed to run and test CRUD operation on CalendarObjects (for an earlier version of our project).
 * <ul>
 * <li> {REPL} defines a static Java class that evaluates user-read input in the form of a Read-Evaluate-Print-Loop, and Creates, Updates, Reads, and Deletes CalendarObjects as desired.
 * <li> {CalendarManager} defines a singleton manager that stores instances of CalendarObjects for usage in REPL class.
 * </ul>
 * @author Jakub Zywicki
 */